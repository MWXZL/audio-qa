# 音频资产检查工具：判据设计与误报治理

一个音频质检工具报出多少问题不重要，重要的是报出来的每一条都站得住。
本文记录 `audio_qa` 从初版到当前版本的六轮判据修正——每一轮都是先在真实素材上
撞出大面积误报，定位到判据本身的错误，再用 ffmpeg 原生命令交叉验证修正结果。

**验证素材**：两批，共 356 个真实素材

第一批 147 个，用于第一到第四轮：
- OpenGameArt RPG Sound Pack：96 个 wav（battle / inventory / interface / NPC / world 分目录）
- Kenney RPG Audio：51 个 ogg（有损格式，用于验证解码路径）

第二批 209 个，用于第五、第六轮：
- Audiokinetic Cube 官方工程 Originals 目录，Wwise 2025.1.10 随附

格式跨度：48 kHz / 44.1 kHz，24 bit / 16 bit，共 5 种格式组合。

第二批的意义在于它**不是免费素材包，而是商业引擎厂商随产品发布的工程**。
在这种素材上报出的大面积缺陷，几乎一定是判据的问题——这个先验假设在第五、第六轮都被证实。

---

## 修正总览

| 判据 | 初版命中 | 当前 | 根本问题 |
|---|---|---|---|
| 响度一致性 | 65/80（81%） | 13/101 | 跨类别比较 + 固定容差 + 把门限下限当真实响度 |
| True Peak | 93/96（97%） | 42（客观过冲） | 把「交付规范余量」当成「物理缺陷」 |
| 削波（有损源） | 22 | 0 FAIL / 16 INFO | 把自己的解码钳位当成素材缺陷 |
| loop 接缝 | 合成正弦即误报 | 0 | 用绝对跳变阈值判高频素材 |
| 头部空白 | 28/209 FAIL | 0 FAIL / 28 INFO | 把音乐分轨当成 Event 触发的一次性音效 |
| 尾部空白 | 37/209 WARN | 12 WARN / 25 INFO | 把自然衰减的残响当成可剪掉的零填充 |
| 重复素材 | 9 组一律 WARN，报 69.9 MB 可回收 | 5 WARN / 1 WARN / 3 INFO，44.5 MB | 把「内容相同」直接当成「可以删一份」 |

---

## 一、loop 接缝：绝对阈值判不了不同频率的素材

### 初版做法

比较首尾样点的差值，超过满刻度 5% 就报循环接缝不连续：

```python
def loop_step(chan, full_scale):
    return abs(chan[0] - chan[-1]) / full_scale
```

### 撞出的问题

一个**人工合成的、数学上完美无缝**的 1 kHz 正弦（时长恰好整数个周期）被判为
接缝跳变 6.52%，超过 5% 阈值。

### 根因

48 kHz 采样下，1 kHz 正弦每个样点推进 7.5°（360° × 1000 / 48000）。
在波形陡峭处，**相邻样点本身**就相差约 6.5% 满刻度：

```
相邻样点差 = 2A·sin(Δθ/2)，其中 Δθ = 7.5°
A = 0.5（测试信号幅度）→ 2 × 0.5 × sin(3.75°) = 6.54% 满刻度
实测 loop_seam 输出 step = 6.52%，理论/实测比 = 1.00
```

也就是说，这个「跳变」是素材固有斜率，不是缺陷。素材越亮、频率越高，
固有斜率越大，越容易被误判。低频素材反而漏报。

### 修正

判据改为**相对素材自身斜率**：接缝跳变 ÷ 内部相邻样点差分的 p99。

```python
def loop_seam(chan, full_scale, sample_stride=1):
    step = abs(chan[0] - chan[-1]) / full_scale
    diffs = [abs(chan[i] - chan[i-1]) / full_scale
             for i in range(1, len(chan), sample_stride)]
    return step, _percentile(diffs, 0.99)
```

- 取 p99 而非最大值：抗单点异常
- 比值上限 3.0 倍：接缝跳变明显超出素材常规斜率才算异常
- 另设 1% 绝对下限：噪声级跳变一律不报

宽带噪声的内部跳变本来就大，参考值随之升高，不会被误报；
而一段平缓素材若首尾错位，比值会显著超标，仍能抓出。

### 验证

单元测试锁定三种情形：同一个跳变信号，命名含 `loop` 判 FAIL、
命名不含判 INFO（提示但不阻断）；数学无缝的正弦完全不产生该 issue。

---

## 二、响度一致性：三个独立的误报源

初版拿全库 LUFS 中位数当唯一基准，固定 ±2 LU 容差。
在 `C:\Windows\Media`（80 个微软系统提示音，专业录制）上命中 65 个，
误报率 81%。一个工具把 81% 的专业素材判成异常，判据必然有问题。

### 2.1 跨类别比较

`C:\Windows\Media` 里既有闹钟（Alarm01-10）又有轻提示音。
**闹钟本来就该比提示音响**——这是设计意图，不是缺陷。
拿全库一个中位数（-27.5 LUFS）当基准，等于要求所有素材一样响。

实测偏差：Alarm02 为 -19.9 LUFS，偏离基准 +7.6 LU，被判异常。
但它作为闹钟，响度合理。

真实工程里响度目标**按类别定**：BGM / SFX / VO / UI 各有各的基准，
从来不存在全库一个数。工程靠目录约定分类，工具应还原同一套约定：

```python
_CATEGORY_DIR_RULES = [...]   # 目录信号优先——目录就是工程的分类依据
_CATEGORY_NAME_RULES = [...]  # 文件名关键词兜底
```

在 147 个素材上分出 7 组：combat 6 / creature 43 / item 15 / ui 6 / misc 5 /
world 1 / uncategorized 25。其中 world 只有 1 个文件，样本不足如实标为「未评估」；
uncategorized 是命名不带类别信息、也不在已知目录下的素材，它的组内 MAD 达 5.6 LU
（其余类别 0.6 ~ 2.7 LU），容差被撑到 16.8 LU——这一组实际上失去了判别力。
这是分类规则的适用边界：**类别推断依赖命名与目录约定，约定不存在时判据只能沉默**，
不能假装自己还在判。

### 2.2 固定容差套在离散素材库上

素材库齐不齐，是这个库自身的属性。拿一个外部固定值（±2 LU）去卡，
本质是在用「我认为它该多齐」代替「它实际有多齐」。

改为容差由**组内离散度**决定：

```python
tolerance = max(cfg.lufs_tolerance, cfg.lufs_spread_k * mad)
```

用 MAD（中位绝对偏差）而非标准差：几个真正的离群文件不会把容差撑大，
判据不会被它要检测的异常本身污染。

同时设组内最小样本数（5 个）：三五个文件的中位数代表不了任何标准，
样本不足的类别如实记为「未评估」，不硬判。

### 2.3 把 EBU R128 门限下限当成真实响度

这一条最隐蔽。28 条响度异常里，**27 条的 LUFS 值都恰好是 -70.0**。

整合响度按 400 ms 门控块统计。短于一个块的素材达不到测量条件，
ffmpeg 直接输出 -70.0 LUFS。这是「测不出」，不是「太轻」。
把它当偏差报，等于把测量能力边界当成素材缺陷。

**实测分界线精确落在 400 ms**：

```
LUFS = -70（不可测组）：46 个，时长 0.138 s ~ 0.3997 s
LUFS 可测组          ：101 个，时长 0.4036 s ~ 3.250 s
```

不可测组最长 0.3997 s，可测组最短 0.4036 s——分界线与 EBU R128 的
门控块长度完全吻合。这不是经验值，是标准本身决定的。

修正：低于门限下限的素材排除在一致性比较之外，
但在报告中如实列出「因短于门控块未参与比较」的文件清单，不静默丢弃。

### 结果

147 个素材上：已测量 101 个，排除 46 个短音效，响度异常 13 个。
误报率从 81% 降到可接受范围，且每个类别的基准、容差、MAD 都在报告里可查。

---

## 三、True Peak：规范余量不是物理缺陷

### 初版做法

默认上限 -1.0 dBTP，超过即 FAIL。

### 撞出的问题

96 个 RPG 素材中 93 个触发，命中率 97%。

### 根因

True Peak 分布是这样的：

```
中位数 -0.10 dBTP
≤ -1（通过）    3 个
-1 ~ 0         73 个
0 ~ +0.5       13 个
> +0.5          7 个
```

中位数 -0.10 说明整个包**归一化到 0 dBFS** ——这是母带的普遍做法。

而 -1 dBTP 是**交付规范的余量要求**，不是物理错误。
拿规范值去硬判一个没有指定规范的素材库，会把一整个正常素材包
判成 97% 不达标。那不是查出问题，是判据用错了场合。

### 修正

区分两种模式，这与响度判据是同一个道理：

**给了显式规范**（`--true-peak-max -1.0`）= 按规范验收，超出即 FAIL，
报告附上超出量，便于修复。

**没给规范** = 只报客观红线：True Peak > 0 dBTP 说明重建波形已越过满刻度，
有损转码（Vorbis / ADPCM）解码后会真削波。这个结论不依赖任何工程约定。
0 ~ -1 dBTP 之间降级为 INFO，说明「余量不足 1 dB，若工程要求 -1 dBTP 需重新归一化」。

### 验证

`interface/interface5.wav` 用 ffmpeg 原生命令交叉验证：

```
$ ffmpeg -i interface5.wav -af ebur128=peak=true -f null -
    I:          -8.2 LUFS
  True peak:
    Peak:        2.1 dBFS
```

工具报告值 +2.1 dBTP，与原生输出一致。

### 结果

FAIL 从 93 降到 42（跨 147 个素材），每一条都是客观越过满刻度，
且工具报告的数字可用原生命令复现。

---

## 四、削波检测：不能把自己的解码钳位当成素材缺陷

### 撞出的问题

51 个 ogg 文件报出 22 条削波，而 96 个 wav 只报 2 条。
有损格式的削波率 10 倍于无损，这个比例本身就不合理。

### 根因

ogg 解码成浮点后，样点本来就可能越过满刻度。实测：

```
$ ffmpeg -i metalPot1.ogg -af astats -f null -
Peak level dB: 1.828533
```

解码峰值 **+1.83 dB**，已越过满刻度。而本工具为了做整数域分析，
把浮点样点转成 int16 时会**削平**这些越界样点——于是产生了成片的
「连续满刻度段」。

这些满刻度段是**我自己的转换造成的**，不是素材在制作时被削波。
真实的越界信号早已由 `true_peak_overshoot` 如实报告。

### 修正

对有损来源（`decoded_via == "ffmpeg"`），整数域的满刻度连续段不作为削波证据，
降级为 INFO 并注明来源是解码钳位。

判断依据用解码路径而非文件扩展名：解码路径是工具内部的事实，
扩展名只是外部标注。

### 结果

`clipping` FAIL 从 22 降到 0（有损源），16 条降级为 `clip_after_decode` INFO。
剩余 2 条 `clipping` FAIL 全部落在 wav 无损路径上，是真实的制作期削波。

---

## 五、首尾空白：两个判据，两种不同的错

前四轮用的是免费素材包。这一轮换了个更硬的靶子：**Audiokinetic 官方 Cube
示例工程**的 209 个 Originals 素材——商业发行的 Wwise 集成示例，
由专业音频团队制作。在这种素材上报出的每一条，举证责任都在工具这边。

初版判据在这 209 个文件上报出 **28 条 `head_silence` FAIL + 37 条
`tail_silence` WARN**，占全库 31%。数字本身就是信号。

### 5.1 头部空白：「触发延迟」这个推论有前提

初版判据：

```python
if head_silence_ms > 100:
    FAIL  "头部空白 N ms，会表现为音效触发延迟"
```

推论本身没错——玩家点击、音效隔 200 ms 才出声，这确实是缺陷。
但它成立需要一个前提：**这个素材是靠 Event 即时触发的一次性音效**。

按时长把 209 个文件分桶，命中分布是决定性的：

| 时长 | 文件数 | head > 100 ms |
|---|---|---|
| 0–1 s | 72 | 0 |
| 1–2 s | 32 | 0 |
| 2–3 s | 17 | 0 |
| 3–5 s | 9 | 0 |
| 5–8 s | 16 | 0 |
| 8–15 s | 20 | 4 |
| 15–40 s | 35 | 19 |
| 40 s+ | 8 | 5 |

**8 秒以下 146 个文件，命中 0 条**。28 条命中最短的一个是 10.43 s，
且文件名全部带 bpm / 拍号 / 小节数标注（`_138bpm4-4_L8M-P0`）——
这些是音乐分轨（stem），不是音效。

最直接的证据来自同一首曲子的三条轨：

```
66.67s  head=    0.0 ms   SFX/Story-Main_90bpm4-4_L24M-P0.wav
66.67s  head=16100.0 ms   SFX/Story-Main-Cello1_90bpm4-4_L24M-P0.wav
66.67s  head=18060.0 ms   SFX/Story-Main-Cello2_90bpm4-4_L24M-P0.wav
```

时长完全相同，主轨头部 0 ms，两条大提琴分轨分别空 16.1 s 和 18.06 s。
按文件名标注的 90 bpm 4/4 计算，一小节 = 4 × 60 / 90 = **2.667 s**：

```
16.10 / 2.667 = 6.04 小节
18.06 / 2.667 = 6.77 小节
```

大提琴在第 6 小节进入。这是编曲，不是缺陷。

**修正**：按时长划界，而不是按文件名关键词——关键词太脆，
换个命名约定就失效。新增 `oneshot_max_duration_s = 8.0`：

```python
is_oneshot = duration_s <= cfg.oneshot_max_duration_s
if head_silence_ms > cfg.head_silence_max_ms:
    if is_oneshot:
        FAIL  head_silence   "一次性音效会表现为触发延迟"
    else:
        INFO  head_offset    "按音乐分轨处理，头部留白通常是声部进入位置"
```

这个思路和第 2.3 节的 `lufs_min_duration_s` 是同一个：
**判据要承认自己的适用边界**。几十秒的素材根本不是靠 Event 即时触发的，
拿「触发延迟」去判它，等于用错误的模型去问一个不适用的问题。

8 s 这条线取自数据本身的空档：命中区最小值 10.43 s，
未命中区最大值 8 s 以下，中间没有样本落在 8–10 s，界限不在争议区。

### 5.2 尾部空白：-60 dB 门限从衰减曲线中间切了一刀

尾部判据的错误性质完全不同，不是「适用边界」而是「测的不是要报的东西」。

初版用 -60 dBFS 门限找尾部静音，超过 500 ms 就报「白占内存与包体」。
但这个推论的前提是那段尾巴**可以剪掉**。逐条测尾部区域里到底有多少是
比特级零、有多少只是低于门限：

```
tail_ms  zero_ms  nonzero_ms  tail_peak_dBFS  path
   4540      883        3657          -61.4   Story-Main-Cello2_90bpm4-4_L24M-P0.wav
   2340        0        2340          -60.2   Story-Main_90bpm4-4_L24M-P0.wav
   1580        0        1580          -61.1   Boss-D Suling Samples/Suling_F#4.wav
   1450        0        1450          -60.8   Boss-D Suling Samples/Suling_D#5.wav
   1360        0        1360          -60.2   Boss-D Suling Samples/Suling_C#5.wav
   1130        0        1130          -60.5   Boss-D Suling Samples/Suling_B4.wav
```

37 条里只有 3 条尾部是真正的比特级零，其余尾部区域**有非零样点**，
且峰值密集落在 **-60.2 ~ -61.7 dBFS**——刚压在门限下方一点点。

这个分布不是巧合：它说明 -60 dBFS 这条线是从一条连续衰减曲线的中间
随便切了一刀。笛子单音（`Suling_*`）尾部那 1.0–1.6 s 是**乐器自然衰减**，
不是废料。剪掉它不省内存，是把一个衰减尾巴硬切成爆音——
判据建议的动作会**制造**一个真缺陷。

**修正**：把「低于门限」和「可无损剪掉」拆成两个量。新增
`trailing_zero_frames()`，只统计末尾**所有声道同时为比特级零**的帧数：

```python
if tail_zero_ms > cfg.tail_silence_max_ms:
    WARN  tail_silence  "尾部比特级零填充 N ms，剪掉无损"
elif tail_silence_ms > cfg.tail_silence_max_ms:
    INFO  tail_decay    "低于门限 N ms，但其中只有 M ms 是比特级零"
```

INFO 那条把两个数字都写出来，读报告的人自己就能判断值不值得动。

### 结果

在 209 个官方素材上：

```
                    修正前                修正后
head_silence   FAIL   28        →    FAIL    0
head_offset    INFO    -        →    INFO   28
tail_silence   WARN   37        →    WARN   12
tail_decay     INFO    -        →    INFO   25

全库汇总    PASS 54 / INFO 83 / WARN 35 / FAIL 37
         →  PASS 54 / INFO 114 / WARN 32 / FAIL 9
```

FAIL 从 37 降到 9，剩下的 9 条是 `clipping` / `true_peak_overshoot` /
`dc_offset`——判据本来就该抓的东西，与首尾空白无关。

存活的 `head_silence` FAIL 为 0：146 个 8 秒以下的一次性音效里
没有一个头部超过 100 ms。这不是判据被削弱了，而是**这批素材在这一项上
本来就是干净的**，专业团队不会让音效带着触发延迟出厂。

保留的 12 条 `tail_silence` WARN 经得起复核，而且指向一个具体成因：

```
17.39s  zero= 869.7 ms  Combat-A-Arpeggio_138bpm4-4-L8M-P0.wav
17.39s  zero= 869.6 ms  Combat-A-LowBrass_138bpm4-4_L8M-P0.wav
17.39s  zero=1739.1 ms  Combat-A-Strings2_138bpm4-4_L8M-P0.wav
```

138 bpm 4/4，一小节 = 4 × 60 / 138 = **1.7391 s**：

```
 869.6 / 1739.1 = 0.500 小节
1739.1 / 1739.1 = 1.000 小节
```

零填充精确等于半小节和整小节——这是导出时按小节边界补齐的结果，
是真零，剪掉无损。判据现在报的是这个，而不是笼统地把 4.5 s 残响
说成 4.5 s 浪费（`Story-Main-Cello2` 那 4540 ms 里只有 883 ms 可剪，
报告里两个数字都在）。

### 测试覆盖

四个新增用例锁住行为边界，其中「衰减尾巴」用指数衰减合成，
末尾电平落到 -60 dB 以下但样点非零，专门用于区分两种尾部：

| 用例 | 断言 |
|---|---|
| 一次性音效头部空白 | 0.3 s 空白 + 0.7 s 正弦 → `head_silence` FAIL |
| 长素材头部空白 | 超过 8 s 的素材 → 降级为 `head_offset` INFO，不出 FAIL |
| 残响尾巴 | 低于门限但非零 → `tail_decay` INFO，不出 `tail_silence` |
| 衰减 + 零填充并存 | 只把真零那段算成浪费，两个数字分别可读 |

全量 42 个测试通过。

---

## 六、重复素材：「内容相同」是事实，「浪费空间」是推论

### 初版做法

两级哈希：文件字节 SHA-256 抓完全相同的文件，解码后 PCM 的 SHA-256
抓「同一段音频装在不同容器里」。字节相同的组，除第一个之外每个都报
`duplicate_asset` WARN，大小累加成「可回收空间」。

### 撞出的问题

Cube 官方工程命中 9 组、10 个文件，报出可回收 69,893,540 B（66.7 MB）。
数字本身没错——这 9 组确实字节级相同。问题是这条缺陷**没法执行**。
把 9 组的文件名并排看：

```
Suling_E#5.wav                          Suling_F5.wav
Combat-Bridge-Gtr1_138bpm3-4_L17M-P0    Combat-Bridge-Gtr1_138bpm4-4_L17M-P1B
Combat-Bridge-Main_138bpm3-4_L17M-P1B   Combat-Bridge-Main_138bpm4-4_L17M-P1B
Combat-Bridge-Gtr2_138bpm3-4_L17M-P0    Combat-Bridge-Gtr2_138bpm4-4_L17M-P1B
                                        Combat-Bridge-Gtr3_138bpm4-4_L17M-P1B
SFX/Story-Main-Cello1_90bpm4-4_L24M-P0  SFX/Story/Story-Main-Cello1_90bpm4-4_L24M-P0
（另外 4 组同为 SFX/ 与 SFX/Story/ 同名文件）
```

第一行 `E#5` 与 `F5` 是**等音异名**——升 E 和 F 是同一个音高，
采样器按音名索引，两个文件都得在，删掉一个就有一个键取不到音。
第二三行只差拍号标注（3/4 与 4/4）和弱起标注，同一段素材在
不同段落被引用，删掉会断掉引用。这三组是**预期行为，不是缺陷**。

第四行不一样：`Gtr2` 和 `Gtr3` 按命名是两个不同的吉他声部，
内容却完全相同——这是真问题，但修法是**重新导出 Gtr3**，
删文件反而把「这里本该有第三个声部」这个信息也删掉了。

只有最后 5 组是真冗余：同名文件同时躺在 `SFX/` 和 `SFX/Story/`，
删一份、把引用改到留下的那份即可。

### 根因

判据把「内容相同」直接等同于「有一份是多余的」，中间跳过了**命名约定**这一层。
文件名不是装饰，它编码了素材身份：音名决定采样器索引位置，
bpm / 拍号 / 小节数 / 弱起标注（`_138bpm4-4_L17M-P1B` = 138 bpm、4/4 拍、
17 小节、弱起 1 拍）决定它在编曲里的角色。两个文件内容相同时，
**名字之间是什么关系**才决定这是预期、是可删、还是该重做。

第二个问题是「可回收空间」把三类混在一起加总。66.7 MB 里有 24.2 MB
属于不该删的文件，报出去等于承诺一个拿不到的收益。

### 修正

先给每组定性，再定级。定性靠两个可计算的判定：

```python
def _note_semitone(token: str) -> int | None:
    """把音名解析成绝对半音号。E#5 与 F5 都是 65，这就是等音异名。"""

def canonical_asset_name(path: str) -> str:
    """去掉「不改变素材身份」的命名成分，得到可比较的规范名。"""
    # 剥掉 bpm/拍号/小节/弱起标注，音名统一折算成半音号
```

三类分别对应不同的 check 名与严重级：

| kind | 判定 | 级别 | 动作 |
|---|---|---|---|
| `redundant_copy` | 文件名相同、所在目录不同 | WARN | 删冗余副本，改引用 |
| `equivalent_naming` | 规范名相同（等音异名 / 仅标注差异） | INFO | 不要删；标注矛盾则统一命名 |
| `duplicate_asset` | 规范名不同，内容相同 | WARN | 复查导出是否漏了声部 |

「可回收空间」只统计 `redundant_copy` 一类。

### 结果

| | 修正前 | 修正后 |
|---|---|---|
| 重复相关命中 | 10 个文件全 WARN | 7 WARN + 3 INFO |
| 可回收空间 | 69,893,540 B（66.7 MB） | 44,491,996 B（42.4 MB） |
| 缺陷可执行性 | 一条笼统的「重复素材」 | 三类，各自对应明确动作 |

差出来的 24.2 MB 正是那三组不该删的文件。剩下 5 组 `redundant_copy`
的 44,491,996 B 可独立核验——Cello1 与 Cello2 各 12,825,758 B、
Story-Main 12,800,740 B、TransFromStory 2,839,000 B、
TransToStory 3,200,740 B，相加正好等于报告里的可回收总量。

Cube 全库汇总的变化只在 WARN/INFO 之间移动了 3 条
（WARN 32 → 29，INFO 114 → 117），FAIL 保持 9 条不变——
这一轮修的是缺陷的**可执行性**，不是漏报或过报。

### 测试覆盖

| 用例 | 断言 |
|---|---|
| 同名跨目录 | → `redundant_copy` WARN，计入可回收空间 |
| 等音异名（`_E#5` / `_F5`） | → `equivalent_naming` INFO，可回收空间为 0 |
| 仅 bpm/拍号标注不同 | → `equivalent_naming` INFO |
| 命名表示不同声部 | → `duplicate_asset` WARN，可回收空间为 0 |

全量 46 个测试通过。

---

## 当前检查结果

### 第一批：147 个免费素材包

```
PASS 27 / INFO 69 / WARN 8 / FAIL 43     耗时 5.7 s
```

FAIL 明细，全部可独立复现：

| 类型 | 数量 | 依据 |
|---|---|---|
| true_peak_overshoot | 40 | True Peak > 0 dBTP，有损转码后必然削波 |
| clipping + overshoot | 2 | 无损源真实削波，且越过满刻度 |
| dc_offset | 1 | 直流偏移 1.93%，超过 1% 上限 |

`misc/random1.wav` 的直流偏移交叉验证：

```
$ ffmpeg -i random1.wav -af astats -f null -
DC offset: 0.019303      (声道 0)
DC offset: 0.020448      (声道 1)
```

工具报告 1.93%，与原生一致。

格式一致性：检出 5 种格式组合（48k/44.1k × 24bit/16bit），
36 个文件偏离主流格式，作为 INFO 提示而非缺陷——
混用采样率是资产管理问题，不是单个文件的错误。

### 第二批：209 个 Cube 官方工程素材

```
PASS 54 / INFO 117 / WARN 29 / FAIL 9    耗时 52.8 s
```

FAIL 从第五轮修正前的 37 条降到 9 条，且 9 条全部不属于第五、六轮讨论的
首尾空白与重复素材类：

| 文件 | 判据 | 实测 |
|---|---|---|
| rumble.wav | dc_offset | 2.31% |
| explode.wav | dc_offset + true_peak_overshoot | 1.21%，+0.1 dBTP |
| Teleport.wav | dc_offset | 1.16% |
| jump_1.wav | dc_offset | 1.16% |
| LightningGem_Blast-01.wav | true_peak_overshoot | +1.9 dBTP |
| LightningGem_Blast-02.wav | clipping + true_peak_overshoot | +1.7 dBTP |
| PoisonGem_Blast-01.wav | true_peak_overshoot | +1.1 dBTP |
| Slimeball_Hit.wav | clipping + true_peak_overshoot | +0.4 dBTP |
| Wwizard_Armor.wav | clipping + true_peak_overshoot | +0.3 dBTP |

四条 dc_offset 用原生 `astats` 逐一交叉验证，与工具报告逐位一致：

```
$ ffmpeg -i rumble.wav   -af astats -f null -    DC offset: 0.023100
$ ffmpeg -i Teleport.wav -af astats -f null -    DC offset: 0.011649
$ ffmpeg -i explode.wav  -af astats -f null -    DC offset: 0.012093
$ ffmpeg -i jump_1.wav   -af astats -f null -    DC offset: 0.011590
```

三条 `clipping` 全部落在 wav 无损路径上，与第四轮的结论一致：
有损源的整数域钳位不算削波，无损源的连续满刻度才算。

这一批的 FAIL 率 4.3%（9/209）。对一个商业引擎厂商随产品发布的示例工程来说，
这个量级是可信的——剩下的都是低频段直流偏移和采样间过冲，
属于素材制作的常见遗留，而不是「这个工程一半的素材有问题」那种判据自身的错误。

---

## 方法论

六轮修正有一条共同的线：**大面积命中通常意味着判据错了，不是素材脏了**。

具体到每一轮，误报的形态不同，但根因是同一类——判据的适用条件没有被检查：

- loop 接缝：用绝对值判一个**依赖频率**的量
- 响度一致性：用全局基准判一个**按类别定义**的量
- True Peak：用交付规范判一个**没有指定规范**的场景
- 削波：用整数域证据判一个**已被自己钳位**的信号
- 首尾空白：用一次性音效的因果链判**音乐分轨**，用「低于门限」代替**「可无损剪掉」**
- 重复素材：把**测得的事实**（内容相同）直接当成了**推论**（有一份多余）

修正的共同手法是让判据**承认自己的适用边界**：
分组、由数据自身定容差、区分规范模式与客观模式、
排除测量能力之外的样本，以及在报告中如实说明哪些没被评估、为什么。

第五、六轮还多出一条：**判据里的每个数字都要对应一个可执行的动作**。
「尾部 4540 ms 低于门限」听起来像缺陷，但音频负责人拿它没法干活——
剪不剪？剪多少？换成「其中 883 ms 是比特级零，可无损剪掉」，
动作就明确了。「66.7 MB 重复素材」也是一样：里面混着不能删的
等音异名采样和该重新导出的漏声部，笼统报出去只会被打回来。
报一个数之前先问它指向什么处理，这一步能滤掉很多看似合理其实无用的判据。

第六轮把这条推到更前面一步：**先分清哪部分是测量，哪部分是解释**。
哈希相同是测量，可以硬下结论；「所以能删」是解释，依赖命名约定这类
工具看不见的上下文。判据只该在测量上强硬，在解释上留出分类和分级。

每一处修正都用 ffmpeg 原生命令（`ebur128` / `astats`）交叉验证过，
报告里的数字与原生输出一致。这是让报告可被质疑、也经得起质疑的前提。
